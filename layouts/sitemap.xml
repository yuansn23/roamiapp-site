{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{- range .Site.RegularPages }}
    {{- if ne .Params.sitemap_exclude true }}
    <url>
      <loc>{{ .Permalink }}</loc>
      {{- $lastmodDate := "" }}
      {{- if eq .Lang "en" }}
        {{- /* 优先使用 .Lastmod，若为零值则回退到 .Date，若仍无则留空（不输出 lastmod） */ -}}
        {{- $lastmodTime := .Lastmod }}
        {{- if $lastmodTime.IsZero }}
          {{- $lastmodTime = .Date }}
        {{- end }}
        {{- if not $lastmodTime.IsZero }}
          {{- $lastmodDate = $lastmodTime.Format "2006-01-02" }}
        {{- end }}
      {{- else }}
        {{- /* 非英文页面：使用构建当天的日期 */ -}}
        {{- $lastmodDate = now.Format "2006-01-02" }}
      {{- end }}
      {{- if ne $lastmodDate "" }}
      <lastmod>{{ $lastmodDate | safeHTML }}</lastmod>
      {{- end }}
    </url>
    {{- end }}
  {{- end }}
</urlset>